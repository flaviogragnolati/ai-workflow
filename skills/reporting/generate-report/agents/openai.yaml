interface:
  display_name: "Generate Quasar Report"
  short_description: "Render Quasar reports to Markdown, DOCX, and PDF"
  default_prompt: "Use $generate-report to render an approved report source as traceable Markdown, DOCX, and PDF documents."
