interface:
  display_name: "Resolve Merge Conflicts"
  short_description: "Resolve Git conflicts while preserving intended behavior"
  default_prompt: "Use $resolve-merge-conflicts to resolve the current Git conflicts while preserving intended behavior."
