interface:
  display_name: "Implement"
  short_description: "Execute settled T3 work and update its source record"
  default_prompt: "Use $implement to execute this settled T3 work, verify it, review it, and update its original record."
